/**
 * Keep entitlement enforcement disabled while product features are under development.
 * Switch this on only when authenticated users and billing state are authoritative.
 */
export const ENFORCE_PRO_LIMITS = false;
